# Engineering Portfolio Manager v7.2.4

- Fixed the lightweight Supabase client to support PATCH updates.
- Added `.single()` response handling for PostgREST object responses.
- Corrected `.select()` chaining so insert/update requests remain write operations.
- Restored resource create, edit, migration, and Supabase synchronization.
