# Release 7.2.1

- Added built-in browser-safe Supabase URL and publishable key configuration.
- Removed device-specific Supabase credential entry and localStorage dependency.
- Supabase now connects automatically on every device.
- Service-role credentials are not included.
