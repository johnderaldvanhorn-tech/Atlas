# Engineering Portfolio Manager v7.2.2

- Resources now load from Supabase on every device.
- Resource create, edit, delete, and CSV import persist to Supabase.
- Existing browser-local resources are migrated automatically when the Supabase resources table is empty.
- Local storage remains a cache, not the system of record.
